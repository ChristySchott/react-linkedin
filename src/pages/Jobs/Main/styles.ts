import styled from 'styled-components';

export const Container = styled.div`
    padding: 25px 22px;
`;

export const Title = styled.h2`
    font-size: 1.6rem;
    line-height: 1.5;
    font-weight: 600;
    color: rgba(0,0,0,.9);
`
