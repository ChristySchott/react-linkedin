import styled from 'styled-components';

export const Container = styled.div`
  margin-top: 35px;
`;

export const Title = styled.span`
    font-size: 1.6rem;
    line-height: 1.5;
    font-weight: 400;
    color: rgba(0,0,0,.9);
`
